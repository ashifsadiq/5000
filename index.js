const express = require('express')
const { inherits } = require('util')
const app = express()

app.get('/', function (req, res) {
    res.send('Hello World')
})

app.listen(5000)
git init
git add.
git commit - m "fkjbf"
git remote add origin git @github.com: ashifsadiq / 5000.git
git branch - M main
git push - u origin main